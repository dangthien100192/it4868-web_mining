export function filterForPhone(phoneMap) {
    // Example filter: Only include phones with a specific property
    // Adjust the filtering logic as needed
    return phoneMap.filter(phone => phone.isActive);
}